# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Utilities
pydantic>=2.5.0
pyyaml>=6.0

# 注意:
# - 核心引擎（core/）为纯 Python 标准库实现，仅依赖 pyyaml
# - HF Spaces Docker 模式无数据库/Redis，相关依赖不在此声明
# - biopython / numpy / pandas / openpyxl / reportlab / matplotlib /
#   pydantic-settings / python-dotenv / loguru 均无代码引用，已移除
