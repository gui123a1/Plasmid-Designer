# Gradio (必须 5.x+，4.x 与新版 huggingface_hub 不兼容)
gradio>=5.0.0,<7.0.0
huggingface_hub>=0.26.0,<2.0.0

# Web Framework (uvicorn 直接启动 Gradio 应用)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# 绘图 (GC分布图)
matplotlib>=3.8.0

# Utilities
pydantic>=2.5.0
pyyaml>=6.0

# 说明：核心引擎（core/）为纯 Python 标准库实现，仅依赖 pyyaml；
# biopython / numpy / pandas / pydantic-settings 均无代码引用，已移除
# （numpy 由 matplotlib 传递安装）。
