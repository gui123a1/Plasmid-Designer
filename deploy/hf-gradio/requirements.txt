# Gradio (必须 5.x+，4.x 与新版 huggingface_hub 不兼容)
gradio>=5.0.0,<7.0.0
huggingface_hub>=0.26.0,<2.0.0

# Web Framework (Gradio 内部使用)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# 绘图 (GC分布图)
matplotlib>=3.8.0

# 核心依赖
biopython>=1.83
numpy>=1.26.0
pandas>=2.2.0
pydantic>=2.5.0
pydantic-settings>=2.0.0
pyyaml>=6.0
