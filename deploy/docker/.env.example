# Plasmid Designer 环境配置

# 数据库配置
DB_USER=plasmid
DB_PASSWORD=plasmid_secure_2026

# Redis配置
REDIS_ENABLED=true

# 安全配置
SECRET_KEY=change_this_in_production_use_strong_random_string
DEBUG=false

# 日志配置
LOG_LEVEL=INFO

# 前端端口（默认80，若被占用可改为其他端口如8080）
FRONTEND_PORT=80
