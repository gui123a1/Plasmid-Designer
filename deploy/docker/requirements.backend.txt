# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9
redis>=5.0.1

# Utilities
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
pyyaml>=6.0
passlib>=1.7.4
# bcrypt 5.0 与 passlib 1.7.4 不兼容（移除 __about__ 探测、72 字节密码改为抛错）
bcrypt>=4.0.0,<5
PyJWT>=2.8.0
email-validator>=2.1.0

# 说明：核心生信算法（core/）为纯 Python 标准库实现，仅需 pyyaml。
# 此前声明的 biopython / primer3-py / pydna / numpy / pandas / scipy /
# openpyxl / reportlab / matplotlib / asyncpg / alembic / loguru 均
# 无代码引用，已移除（镜像体积显著减小）。
