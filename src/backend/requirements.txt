# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database（STORAGE_MODE=database 时使用；本地开发默认 SQLite，无需额外驱动）
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9
redis>=5.0.1

# Utilities
pyyaml>=6.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
passlib>=1.7.4
bcrypt>=4.0.0
PyJWT>=2.8.0
email-validator>=2.1.0

# Testing
pytest>=7.4.0
httpx>=0.26.0

# 说明：核心生信算法（core/）为纯 Python 标准库实现，
# 不依赖 BioPython / primer3-py / pydna / numpy / pandas / scipy 等第三方库。
